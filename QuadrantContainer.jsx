import "./QuadrantContainer.css";

// eslint-disable-next-line react/prop-types
function QuadrantContainer({ children }) {
  return <div className="quadrant-container">{children}</div>;
}

export default QuadrantContainer;
